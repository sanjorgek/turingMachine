machine:
  ghc:
    version: 7.10.1

dependencies:
  override:
    - cabal update
    - cabal sandbox init
    - cabal install --enable-tests --enable-benchmarks
    - cabal configure --enable-tests --enable-benchmarks

test:
  post:
    - cabal bench
